# Copy to .env for local runs (and set these in Netlify site settings for deploy).
# Use the SAME values as the Next.js app's NEXT_PUBLIC_SUPABASE_URL / _ANON_KEY.
SUPABASE_URL=https://cyzpjcntakgxcjnwlrnp.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Local standalone server port (src/index.ts). Optional.
PORT=3333
